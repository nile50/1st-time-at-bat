# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a minimal Python project in its earliest stage. Currently the only source file is `README.md`, which contains a single Python print statement:

```python
print("Good morning Vietnam!")
```

The project is MIT-licensed and owned by nile50.

## Current State

- No build system, package manager, or dependencies exist yet.
- No test framework is configured.
- No linter or formatter is configured.
- The project has two commits: an initial commit and the addition of the greeting message.

## Running the Code

```bash
python README.md   # or: python3 README.md
```

As the project grows, this file should be updated to reflect the actual build, test, and lint commands introduced.
